# AI-ATLAS default PII field name patterns.
# Each line is a Java regex fragment (case-insensitive matching).
# Blank lines and lines starting with # are ignored.
#
# To customize, provide your own file via the processor option:
#   -Aai.atlas.pii.patterns.file=/path/to/your-patterns.conf
#
# When a custom file is provided it REPLACES these defaults entirely.
# The additive option -Aai.atlas.pii.patterns=keyword1,keyword2 still
# works on top of whichever base set is active.

ssn
password
passwd
credit.?card
card.?number
cvv
cvc
tax.?id
driver.?license
passport
secret
