server:
  port: 8080

spring:
  application:
    name: ai-atlas-demo
  ai:
    mcp:
      server:
        name: ai-atlas-demo
        version: 0.1.0

ai:
  adam:
    mcp:
      enabled: true
    audit:
      enabled: true

logging:
  level:
    ai.atlas: DEBUG
