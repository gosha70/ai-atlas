# Origin alignment check — contract-quality-foundations

Origin: https://github.com/gosha70/ai-atlas/issues/23 (read in full, including "Suggested issue split" and "Priority / recommended sequencing") + specs/contract-quality-foundations/origin/2026-09-24-owner-decisions.md (the owner's four decisions and the owner's review of the first draft)

Origin claim:
> Epic gosha70/ai-atlas#23 moves AI-ATLAS "from deterministic interface generation to governed interface lifecycle management" and asks to "keep this as the epic and create focused child issues for implementation", in its stated order: Phase 0, "Fix generated REST ↔ OpenAPI parameter-contract mismatch" ("Immediate correctness prerequisite"), then the Phase 1 items "MCP tool-name collision detection", "Per-method AI tool description diagnostics" and "Streamable HTTP transport alongside SSE". Phase 0 "should state which representation is canonical and treat the correction as a declared change rather than a silent one". The owner asked on 2026-09-24 to set up Claude Code to implement the epic, approved splitting it with this feature (Phase 0 + the three Phase 1 items) first, and chose query parameters as canonical, SSE as the default transport with Streamable HTTP opt-in, and a single `ai.atlas.strict` option for strict mode.

Working claim:
> spec.md (revised after two owner reviews) delivers the epic's Phase 0 and all three Phase 1 items. Phase 0 (FR-001..008): the generated OpenAPI describes the generated controllers exactly — operations merged per path so none is lost, duplicate (method, path) mappings anywhere in the compilation a compile error, arguments as required `in: query` parameters with no requestBody, unique operationIds assigned in two passes against the whole document (unique names unchanged), responses per return kind — and `void` methods generate compiling controller and MCP code, the one allowed generated-code change; consistency is tested both ways at compile time (including the owner's `find()`/`find(Long id)` and `OrderService_find_get()` fixtures and a `void` fixture) and at runtime; the correction is declared in CHANGELOG and docs. This meets the epic's Phase 0 acceptance criteria: same HTTP method, path, parameter locations, content types and status codes; golden/integration tests; the canonical representation chosen and documented with consumer impact. Phase 1: tool-name collisions are compile errors at every site with unique names unchanged (FR-009..011, epic §2); missing method descriptions warn, error under `ai.atlas.strict`, API-only unaffected, version/deprecation guidance kept (FR-012..015, epic §3); Streamable HTTP opt-in, SSE default unchanged, STDIO unchanged, documented (FR-016..018, epic §4). FR-019 requires both CI JDK legs, run by an executable verifier. Phases 2–5 are out of scope per the epic's split.

Mismatches:
  - none. The feature is a subset of the epic by design, as the epic asks and the owner approved. The additions since the first draft (void fix, path merging, operationId uniqueness, duplicate-mapping error, two-JDK verifier) come from the owner's review and all serve the epic's Phase 0 criterion that "OpenAPI describes the same HTTP method, path, parameter locations, content types, and response status codes as the generated controller". Cross-compilation collision detection needs the Phase 2 Contract IR (plan ADR-3).

Verdict: aligned
Confidence: high
