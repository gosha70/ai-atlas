# Origin alignment check — contract-ir-gate

Origin: https://github.com/gosha70/ai-atlas/issues/23 §1 and the "Contract acceptance / lock mode" child (read in full this session) + specs/contract-ir-gate/origin/2026-09-25-owner-decisions.md

Origin claim:
> Epic gosha70/ai-atlas#23 §1 asks to "introduce a distinct, framework-neutral Contract IR containing every declared contract element and its lifecycle metadata, then derive the active API version from that IR", persisted as `.atlas/api.ir.json` with "its own schema version", and a build that will "derive the fresh Contract IR; diff it against the committed baseline; classify changes; reconcile breaking changes with lifecycle annotations; fail with a precise diagnostic when a breaking delta is undeclared", with direction-aware rules, enum openness, and an optional "contract-lock mode" that requires "explicit acceptance of any delta via an atlasAccept-style task". Its design constraints require that "REST, MCP, DTOs, and OpenAPI must be projections of the same normalised contract model". On 2026-09-25 the owner asked to proceed with the epic and chose: one feature covering IR, gate, accept and lock; the gate protects the baseline's published major; response enums closed by default; and the generators rewired to project from the IR now.

Working claim:
> spec.md delivers a deterministic, version-neutral Contract IR of every entity, field and operation with lifecycle, including inactive elements, carrying irVersion 1 with a documented migration policy and no framework types (FR-001..005, epic §1 criteria: deterministic and byte-stable, all declarations and lifecycle, own schema version). Every generator consumes the IR projected at the configured major, with output proven byte-identical by a golden snapshot captured before the rewire (FR-006..007, epic design constraint: projections of one model). A compile-time gate compares baseline and fresh IR projected at the baseline's published major, classifying outputs and inputs separately. Removals, renames, type changes, parameter-list changes, channel/tool/REST changes, removed input enum values and values added to closed response enums are breaking. Declared lifecycle transitions and additions are compatible. `openEnum` declares tolerant response enums. Errors name the element, the change and the legitimising declaration, and a deterministic contract-diff.json lists every difference (FR-008..013, epic criteria: removal of an @AgenticField fails naming the field path; removed operation, narrowed input type, removed accepted enum value and added required input detected; input and output classified independently; enum openness; report names the element and the legitimising annotation). Lock mode fails on any whole-document difference until `atlasAccept` rewrites the baseline, which works while the build is failing (FR-014..016, epic criterion: lock mode fails on any unaccepted delta, cleared only by explicit accept). The CLI and MCP surface gate failures (FR-017, epic: cli/mcp-stdio "should surface the same gate exit codes"). The demo is governed by a committed baseline, and the docs cover the rules (FR-018..019). The build is green on both JDKs (FR-020). Everything is offline (epic criterion: no live DB, network call, or LLM).

Mismatches:
  - none. Owner-chosen scoping within the epic's options: the gate protects the baseline's published major rather than all historical majors; response enums are closed by default with `openEnum` to opt out; the generators are rewired now. Constraint normalisation ("narrowing an input constraint") depends on Phase 3's constraint model and is out of scope; type narrowing is detected. Release snapshots are Phase 5.

Verdict: aligned
Confidence: high
